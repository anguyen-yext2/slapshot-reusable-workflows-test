name: Run Script from Env
description: Executes a multi-line shell script from an environment or input variable
inputs:
  script:
    description: The shell script content to run
    required: true
runs:
  using: 'node20'
  main: 'dist/index.js'